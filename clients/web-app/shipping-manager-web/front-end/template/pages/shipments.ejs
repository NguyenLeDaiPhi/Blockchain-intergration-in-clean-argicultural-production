<!DOCTYPE html>
<html lang="vi">
<%- include('../partials/header') %>
<body>
    <div class="d-flex">
        <%- include('../partials/sidebar') %>
        
        <div class="flex-grow-1 p-4" style="margin-left: 250px; background-color: #f8f9fa; min-height: 100vh;">
            <h2 class="mb-4">🚚 Quản lý Vận chuyển</h2>
            
            <div class="card shadow-sm">
                <div class="card-body">
                    <table class="table align-middle">
                        <thead>
                            <tr>
                                <th>Mã Vận Đơn</th>
                                <th>Mã Đơn Hàng</th>
                                <th>Tài xế / Xe</th>
                                <th>Trạng thái</th>
                                <th>Hành động</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% if (shipments && shipments.length > 0) { %>
                                <% shipments.forEach(function(ship) { %>
                                    <tr>
                                        <td><strong><%= ship.id %></strong></td>
                                        <td>#<%= ship.orderId %></td>
                                        <td>
                                            <% if (ship.driverId) { %>
                                                <div><%= ship.driverName %></div>
                                                <small class="text-muted"><%= ship.vehiclePlate %></small>
                                            <% } else { %>
                                                <span class="text-danger">Chưa gán</span>
                                            <% } %>
                                        </td>
                                        <td>
                                            <span class="badge bg-<%= ship.status === 'PENDING' ? 'warning' : (ship.status === 'IN_TRANSIT' ? 'primary' : 'success') %>">
                                                <%= ship.status %>
                                            </span>
                                        </td>
                                        <td>
                                            <% if (ship.status === 'PENDING') { %>
                                                <button type="button" class="btn btn-sm btn-outline-primary" data-bs-toggle="modal" data-bs-target="#assignModal<%= ship.id %>">
                                                    Gán xe
                                                </button>
                                                
                                                <!-- Modal Gán Xe -->
                                                <div class="modal fade" id="assignModal<%= ship.id %>" tabindex="-1">
                                                    <div class="modal-dialog">
                                                        <form action="/shipments/assign" method="POST" class="modal-content">
                                                            <div class="modal-header">
                                                                <h5 class="modal-title">Gán xe cho đơn <%= ship.id %></h5>
                                                                <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
                                                            </div>
                                                            <div class="modal-body">
                                                                <input type="hidden" name="shipmentId" value="<%= ship.id %>">
                                                                <div class="mb-3">
                                                                    <label class="form-label">Chọn Tài xế</label>
                                                                    <select name="driverId" class="form-select" required>
                                                                        <% drivers.forEach(d => { %>
                                                                            <option value="<%= d.id %>"><%= d.name %> (<%= d.license %>)</option>
                                                                        <% }) %>
                                                                    </select>
                                                                </div>
                                                                <div class="mb-3">
                                                                    <label class="form-label">Chọn Xe</label>
                                                                    <select name="vehicleId" class="form-select" required>
                                                                        <% vehicles.forEach(v => { %>
                                                                            <option value="<%= v.id %>"><%= v.plate %> - <%= v.type %></option>
                                                                        <% }) %>
                                                                    </select>
                                                                </div>
                                                            </div>
                                                            <div class="modal-footer">
                                                                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Đóng</button>
                                                                <button type="submit" class="btn btn-primary">Lưu</button>
                                                            </div>
                                                        </form>
                                                    </div>
                                                </div>
                                            <% } %>
                                        </td>
                                    </tr>
                                <% }); %>
                            <% } else { %>
                                <tr><td colspan="5" class="text-center">Chưa có chuyến vận chuyển nào.</td></tr>
                            <% } %>
                        </tbody>
                    </table>
                </div>
            </div>
            <%- include('../partials/footer') %>
        </div>
    </div>
</body>
</html>
