_format_version: "3.0"
_transform: true

services:
  - name: auth-service
    url: http://auth-service:8080

routes:
  - name: auth-routes
    service: auth-service
    paths:
      - /api/auth
    strip_path: false   
    methods:            
      - GET
      - POST
      - OPTIONS